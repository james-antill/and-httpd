# Makefile.in generated by automake 1.9.5 from Makefile.am.
# @configure_input@

# Copyright (C) 1994, 1995, 1996, 1997, 1998, 1999, 2000, 2001, 2002,
# 2003, 2004, 2005  Free Software Foundation, Inc.
# This Makefile.in is free software; the Free Software Foundation
# gives unlimited permission to copy and/or distribute it,
# with or without modifications, as long as this notice is preserved.

# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY, to the extent permitted by law; without
# even the implied warranty of MERCHANTABILITY or FITNESS FOR A
# PARTICULAR PURPOSE.

@SET_MAKE@


srcdir = @srcdir@
top_srcdir = @top_srcdir@
VPATH = @srcdir@
pkgdatadir = $(datadir)/@PACKAGE@
pkglibdir = $(libdir)/@PACKAGE@
pkgincludedir = $(includedir)/@PACKAGE@
top_builddir = ..
am__cd = CDPATH="$${ZSH_VERSION+.}$(PATH_SEPARATOR)" && cd
INSTALL = @INSTALL@
install_sh_DATA = $(install_sh) -c -m 644
install_sh_PROGRAM = $(install_sh) -c
install_sh_SCRIPT = $(install_sh) -c
INSTALL_HEADER = $(INSTALL_DATA)
transform = $(program_transform_name)
NORMAL_INSTALL = :
PRE_INSTALL = :
POST_INSTALL = :
NORMAL_UNINSTALL = :
PRE_UNINSTALL = :
POST_UNINSTALL = :
build_triplet = @build@
host_triplet = @host@
subdir = tools
DIST_COMMON = README $(srcdir)/Makefile.am $(srcdir)/Makefile.in \
	$(srcdir)/and-httpd.in $(srcdir)/and-httpd_gzip.in \
	$(srcdir)/and-httpd_logs.in $(srcdir)/and-httpd_status.in \
	$(srcdir)/make_index.in \
	$(srcdir)/scons_and_httpd_helpers.py.in
ACLOCAL_M4 = $(top_srcdir)/aclocal.m4
am__aclocal_m4_deps = $(top_srcdir)/configure.in
am__configure_deps = $(am__aclocal_m4_deps) $(CONFIGURE_DEPENDENCIES) \
	$(ACLOCAL_M4)
mkinstalldirs = $(install_sh) -d
CONFIG_HEADER = $(top_builddir)/src/autoconf.h
CONFIG_CLEAN_FILES = and-httpd and-httpd_gzip and-httpd_logs \
	and-httpd_status make_index scons_and_httpd_helpers.py
am__installdirs = "$(DESTDIR)$(cronddir)" "$(DESTDIR)$(initddir)" \
	"$(DESTDIR)$(toolsmaindir)" "$(DESTDIR)$(confdir)" \
	"$(DESTDIR)$(confddir)" "$(DESTDIR)$(toolsmaindir)" \
	"$(DESTDIR)$(wwwconfdir)" "$(DESTDIR)$(wwwgendir)" \
	"$(DESTDIR)$(wwwhtmldir)"
crondSCRIPT_INSTALL = $(INSTALL_SCRIPT)
initdSCRIPT_INSTALL = $(INSTALL_SCRIPT)
toolsmainSCRIPT_INSTALL = $(INSTALL_SCRIPT)
SCRIPTS = $(crond_SCRIPTS) $(initd_SCRIPTS) $(toolsmain_SCRIPTS)
SOURCES =
DIST_SOURCES =
am__vpath_adj_setup = srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`;
am__vpath_adj = case $$p in \
    $(srcdir)/*) f=`echo "$$p" | sed "s|^$$srcdirstrip/||"`;; \
    *) f=$$p;; \
  esac;
am__strip_dir = `echo $$p | sed -e 's|^.*/||'`;
confDATA_INSTALL = $(INSTALL_DATA)
confdDATA_INSTALL = $(INSTALL_DATA)
toolsmainDATA_INSTALL = $(INSTALL_DATA)
wwwconfDATA_INSTALL = $(INSTALL_DATA)
wwwgenDATA_INSTALL = $(INSTALL_DATA)
wwwhtmlDATA_INSTALL = $(INSTALL_DATA)
DATA = $(conf_DATA) $(confd_DATA) $(toolsmain_DATA) $(wwwconf_DATA) \
	$(wwwgen_DATA) $(wwwhtml_DATA)
DISTFILES = $(DIST_COMMON) $(DIST_SOURCES) $(TEXINFOS) $(EXTRA_DIST)
ACLOCAL = @ACLOCAL@
ALLOCA = @ALLOCA@
AMDEP_FALSE = @AMDEP_FALSE@
AMDEP_TRUE = @AMDEP_TRUE@
AMTAR = @AMTAR@
AR = @AR@
AUTOCONF = @AUTOCONF@
AUTOHEADER = @AUTOHEADER@
AUTOMAKE = @AUTOMAKE@
AWK = @AWK@
BZIP2_PATH = @BZIP2_PATH@
CC = @CC@
CCAS = @CCAS@
CCASFLAGS = @CCASFLAGS@
CCDEPMODE = @CCDEPMODE@
CFLAGS = @CFLAGS@
CHMOD_PATH = @CHMOD_PATH@
CPP = @CPP@
CPPFLAGS = @CPPFLAGS@
CXX = @CXX@
CXXCPP = @CXXCPP@
CXXDEPMODE = @CXXDEPMODE@
CXXFLAGS = @CXXFLAGS@
CYGPATH_W = @CYGPATH_W@
DBG_TIMER_Q_FALSE = @DBG_TIMER_Q_FALSE@
DBG_TIMER_Q_TRUE = @DBG_TIMER_Q_TRUE@
DBG_VSTR_FALSE = @DBG_VSTR_FALSE@
DBG_VSTR_TRUE = @DBG_VSTR_TRUE@
DEFS = @DEFS@
DEPDIR = @DEPDIR@
ECHO = @ECHO@
ECHO_C = @ECHO_C@
ECHO_N = @ECHO_N@
ECHO_T = @ECHO_T@
EGREP = @EGREP@
ETAGS_PATH = @ETAGS_PATH@
EXEEXT = @EXEEXT@
F77 = @F77@
FFLAGS = @FFLAGS@
GZIP_PATH = @GZIP_PATH@
HAVE_LINKER_MAIN_RELOCATE_FALSE = @HAVE_LINKER_MAIN_RELOCATE_FALSE@
HAVE_LINKER_MAIN_RELOCATE_TRUE = @HAVE_LINKER_MAIN_RELOCATE_TRUE@
INSTALL_DATA = @INSTALL_DATA@
INSTALL_PROGRAM = @INSTALL_PROGRAM@
INSTALL_SCRIPT = @INSTALL_SCRIPT@
INSTALL_STRIP_PROGRAM = @INSTALL_STRIP_PROGRAM@
LDFLAGS = @LDFLAGS@
LIBCAP_AVAIL_FALSE = @LIBCAP_AVAIL_FALSE@
LIBCAP_AVAIL_TRUE = @LIBCAP_AVAIL_TRUE@
LIBOBJS = @LIBOBJS@
LIBS = @LIBS@
LIBTOOL = @LIBTOOL@
LIBTOOL_DEPS = @LIBTOOL_DEPS@
LN_S = @LN_S@
LTLIBOBJS = @LTLIBOBJS@
MAKEINFO = @MAKEINFO@
MAKE_PATH = @MAKE_PATH@
MALLINFO_AVAIL_FALSE = @MALLINFO_AVAIL_FALSE@
MALLINFO_AVAIL_TRUE = @MALLINFO_AVAIL_TRUE@
NDEBUG_FALSE = @NDEBUG_FALSE@
NDEBUG_TRUE = @NDEBUG_TRUE@
OBJEXT = @OBJEXT@
PACKAGE = @PACKAGE@
PACKAGE_BUGREPORT = @PACKAGE_BUGREPORT@
PACKAGE_NAME = @PACKAGE_NAME@
PACKAGE_STRING = @PACKAGE_STRING@
PACKAGE_TARNAME = @PACKAGE_TARNAME@
PACKAGE_VERSION = @PACKAGE_VERSION@
PATH_SEPARATOR = @PATH_SEPARATOR@
PERL_PATH = @PERL_PATH@
PERL_USR_BIN_AVAIL_FALSE = @PERL_USR_BIN_AVAIL_FALSE@
PERL_USR_BIN_AVAIL_TRUE = @PERL_USR_BIN_AVAIL_TRUE@
PKG_CONFIG = @PKG_CONFIG@
RANLIB = @RANLIB@
SED_PATH = @SED_PATH@
SENDMAIL_PATH = @SENDMAIL_PATH@
SET_MAKE = @SET_MAKE@
SHELL = @SHELL@
SOCKET_POLL_AVAIL_FALSE = @SOCKET_POLL_AVAIL_FALSE@
SOCKET_POLL_AVAIL_TRUE = @SOCKET_POLL_AVAIL_TRUE@
SOCKET_POLL_CFLAGS = @SOCKET_POLL_CFLAGS@
SOCKET_POLL_LDLIBS = @SOCKET_POLL_LDLIBS@
STRIP = @STRIP@
TAR_PATH = @TAR_PATH@
TIMER_Q_AVAIL_FALSE = @TIMER_Q_AVAIL_FALSE@
TIMER_Q_AVAIL_TRUE = @TIMER_Q_AVAIL_TRUE@
TIMER_Q_CFLAGS = @TIMER_Q_CFLAGS@
TIMER_Q_DBG_AVAIL_FALSE = @TIMER_Q_DBG_AVAIL_FALSE@
TIMER_Q_DBG_AVAIL_TRUE = @TIMER_Q_DBG_AVAIL_TRUE@
TIMER_Q_DBG_CFLAGS = @TIMER_Q_DBG_CFLAGS@
TIMER_Q_DBG_LDLIBS = @TIMER_Q_DBG_LDLIBS@
TIMER_Q_LDLIBS = @TIMER_Q_LDLIBS@
TRUE_PATH = @TRUE_PATH@
VERSION = @VERSION@
VSTR_AVAIL_FALSE = @VSTR_AVAIL_FALSE@
VSTR_AVAIL_TRUE = @VSTR_AVAIL_TRUE@
VSTR_CFLAGS = @VSTR_CFLAGS@
VSTR_DBG_AVAIL_FALSE = @VSTR_DBG_AVAIL_FALSE@
VSTR_DBG_AVAIL_TRUE = @VSTR_DBG_AVAIL_TRUE@
VSTR_DBG_CFLAGS = @VSTR_DBG_CFLAGS@
VSTR_DBG_LDLIBS = @VSTR_DBG_LDLIBS@
VSTR_LDLIBS = @VSTR_LDLIBS@
WARNS = @WARNS@
ac_ct_AR = @ac_ct_AR@
ac_ct_CC = @ac_ct_CC@
ac_ct_CXX = @ac_ct_CXX@
ac_ct_F77 = @ac_ct_F77@
ac_ct_RANLIB = @ac_ct_RANLIB@
ac_ct_STRIP = @ac_ct_STRIP@
am__fastdepCC_FALSE = @am__fastdepCC_FALSE@
am__fastdepCC_TRUE = @am__fastdepCC_TRUE@
am__fastdepCXX_FALSE = @am__fastdepCXX_FALSE@
am__fastdepCXX_TRUE = @am__fastdepCXX_TRUE@
am__include = @am__include@
am__leading_dot = @am__leading_dot@
am__quote = @am__quote@
am__tar = @am__tar@
am__untar = @am__untar@
bindir = @bindir@
build = @build@
build_alias = @build_alias@
build_cpu = @build_cpu@
build_os = @build_os@
build_vendor = @build_vendor@
datadir = @datadir@
exec_prefix = @exec_prefix@
host = @host@
host_alias = @host_alias@
host_cpu = @host_cpu@
host_os = @host_os@
host_vendor = @host_vendor@
includedir = @includedir@
infodir = @infodir@
install_sh = @install_sh@
libdir = @libdir@
libexecdir = @libexecdir@
localstatedir = @localstatedir@
mandir = @mandir@
mkdir_p = @mkdir_p@
oldincludedir = @oldincludedir@
prefix = @prefix@
program_transform_name = @program_transform_name@
sbindir = @sbindir@
sharedstatedir = @sharedstatedir@
sysconfdir = @sysconfdir@
target_alias = @target_alias@
toolsmaindir = $(datadir)/and-httpd-$(VERSION)-tools
toolsmain_DATA = scons_and_httpd_helpers.py
toolsmain_SCRIPTS = \
                and-httpd-status \
                and-httpd-syslog-grep \
                and-httpd-syslog2apache-httpd-combined-log \
                conv_html2text \
                gzip-r \
                make_index

initddir = $(sysconfdir)/init.d
initd_SCRIPTS = and-httpd
cronddir = $(sysconfdir)/cron.hourly
crond_SCRIPTS = and-httpd_gzip and-httpd_status and-httpd_logs
confdir = $(sysconfdir)/and-httpd
conf_DATA = and-httpd.conf mime_types_extra.txt
confddir = $(sysconfdir)/and-httpd/conf.d
confd_DATA = README _redirect-index.conf _,tools.conf _io-limiting.conf
wwwconfdir = $(localstatedir)/www/conf
wwwconf_DATA = status status.html status.html.gz status.html.bz2
wwwhtmldir = $(localstatedir)/www/html
wwwhtml_DATA = 
wwwgendir = $(localstatedir)/www/generated_html
wwwgen_DATA = 

# Generated from autoconf
CLEANFILES = $(initd_SCRIPTS)
all: all-am

.SUFFIXES:
$(srcdir)/Makefile.in:  $(srcdir)/Makefile.am  $(am__configure_deps)
	@for dep in $?; do \
	  case '$(am__configure_deps)' in \
	    *$$dep*) \
	      cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh \
		&& exit 0; \
	      exit 1;; \
	  esac; \
	done; \
	echo ' cd $(top_srcdir) && $(AUTOMAKE) --gnu  tools/Makefile'; \
	cd $(top_srcdir) && \
	  $(AUTOMAKE) --gnu  tools/Makefile
.PRECIOUS: Makefile
Makefile: $(srcdir)/Makefile.in $(top_builddir)/config.status
	@case '$?' in \
	  *config.status*) \
	    cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh;; \
	  *) \
	    echo ' cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe)'; \
	    cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@ $(am__depfiles_maybe);; \
	esac;

$(top_builddir)/config.status: $(top_srcdir)/configure $(CONFIG_STATUS_DEPENDENCIES)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh

$(top_srcdir)/configure:  $(am__configure_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
$(ACLOCAL_M4):  $(am__aclocal_m4_deps)
	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
and-httpd: $(top_builddir)/config.status $(srcdir)/and-httpd.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
and-httpd_gzip: $(top_builddir)/config.status $(srcdir)/and-httpd_gzip.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
and-httpd_logs: $(top_builddir)/config.status $(srcdir)/and-httpd_logs.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
and-httpd_status: $(top_builddir)/config.status $(srcdir)/and-httpd_status.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
make_index: $(top_builddir)/config.status $(srcdir)/make_index.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
scons_and_httpd_helpers.py: $(top_builddir)/config.status $(srcdir)/scons_and_httpd_helpers.py.in
	cd $(top_builddir) && $(SHELL) ./config.status $(subdir)/$@
install-crondSCRIPTS: $(crond_SCRIPTS)
	@$(NORMAL_INSTALL)
	test -z "$(cronddir)" || $(mkdir_p) "$(DESTDIR)$(cronddir)"
	@list='$(crond_SCRIPTS)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  if test -f $$d$$p; then \
	    f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	    echo " $(crondSCRIPT_INSTALL) '$$d$$p' '$(DESTDIR)$(cronddir)/$$f'"; \
	    $(crondSCRIPT_INSTALL) "$$d$$p" "$(DESTDIR)$(cronddir)/$$f"; \
	  else :; fi; \
	done

uninstall-crondSCRIPTS:
	@$(NORMAL_UNINSTALL)
	@list='$(crond_SCRIPTS)'; for p in $$list; do \
	  f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	  echo " rm -f '$(DESTDIR)$(cronddir)/$$f'"; \
	  rm -f "$(DESTDIR)$(cronddir)/$$f"; \
	done
install-initdSCRIPTS: $(initd_SCRIPTS)
	@$(NORMAL_INSTALL)
	test -z "$(initddir)" || $(mkdir_p) "$(DESTDIR)$(initddir)"
	@list='$(initd_SCRIPTS)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  if test -f $$d$$p; then \
	    f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	    echo " $(initdSCRIPT_INSTALL) '$$d$$p' '$(DESTDIR)$(initddir)/$$f'"; \
	    $(initdSCRIPT_INSTALL) "$$d$$p" "$(DESTDIR)$(initddir)/$$f"; \
	  else :; fi; \
	done

uninstall-initdSCRIPTS:
	@$(NORMAL_UNINSTALL)
	@list='$(initd_SCRIPTS)'; for p in $$list; do \
	  f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	  echo " rm -f '$(DESTDIR)$(initddir)/$$f'"; \
	  rm -f "$(DESTDIR)$(initddir)/$$f"; \
	done
install-toolsmainSCRIPTS: $(toolsmain_SCRIPTS)
	@$(NORMAL_INSTALL)
	test -z "$(toolsmaindir)" || $(mkdir_p) "$(DESTDIR)$(toolsmaindir)"
	@list='$(toolsmain_SCRIPTS)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  if test -f $$d$$p; then \
	    f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	    echo " $(toolsmainSCRIPT_INSTALL) '$$d$$p' '$(DESTDIR)$(toolsmaindir)/$$f'"; \
	    $(toolsmainSCRIPT_INSTALL) "$$d$$p" "$(DESTDIR)$(toolsmaindir)/$$f"; \
	  else :; fi; \
	done

uninstall-toolsmainSCRIPTS:
	@$(NORMAL_UNINSTALL)
	@list='$(toolsmain_SCRIPTS)'; for p in $$list; do \
	  f=`echo "$$p" | sed 's|^.*/||;$(transform)'`; \
	  echo " rm -f '$(DESTDIR)$(toolsmaindir)/$$f'"; \
	  rm -f "$(DESTDIR)$(toolsmaindir)/$$f"; \
	done

mostlyclean-libtool:
	-rm -f *.lo

clean-libtool:
	-rm -rf .libs _libs

distclean-libtool:
	-rm -f libtool
uninstall-info-am:
install-confDATA: $(conf_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(confdir)" || $(mkdir_p) "$(DESTDIR)$(confdir)"
	@list='$(conf_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(confDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(confdir)/$$f'"; \
	  $(confDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(confdir)/$$f"; \
	done

uninstall-confDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(conf_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(confdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(confdir)/$$f"; \
	done
install-confdDATA: $(confd_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(confddir)" || $(mkdir_p) "$(DESTDIR)$(confddir)"
	@list='$(confd_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(confdDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(confddir)/$$f'"; \
	  $(confdDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(confddir)/$$f"; \
	done

uninstall-confdDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(confd_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(confddir)/$$f'"; \
	  rm -f "$(DESTDIR)$(confddir)/$$f"; \
	done
install-toolsmainDATA: $(toolsmain_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(toolsmaindir)" || $(mkdir_p) "$(DESTDIR)$(toolsmaindir)"
	@list='$(toolsmain_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(toolsmainDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(toolsmaindir)/$$f'"; \
	  $(toolsmainDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(toolsmaindir)/$$f"; \
	done

uninstall-toolsmainDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(toolsmain_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(toolsmaindir)/$$f'"; \
	  rm -f "$(DESTDIR)$(toolsmaindir)/$$f"; \
	done
install-wwwconfDATA: $(wwwconf_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wwwconfdir)" || $(mkdir_p) "$(DESTDIR)$(wwwconfdir)"
	@list='$(wwwconf_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wwwconfDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wwwconfdir)/$$f'"; \
	  $(wwwconfDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wwwconfdir)/$$f"; \
	done

uninstall-wwwconfDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wwwconf_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wwwconfdir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wwwconfdir)/$$f"; \
	done
install-wwwgenDATA: $(wwwgen_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wwwgendir)" || $(mkdir_p) "$(DESTDIR)$(wwwgendir)"
	@list='$(wwwgen_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wwwgenDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wwwgendir)/$$f'"; \
	  $(wwwgenDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wwwgendir)/$$f"; \
	done

uninstall-wwwgenDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wwwgen_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wwwgendir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wwwgendir)/$$f"; \
	done
install-wwwhtmlDATA: $(wwwhtml_DATA)
	@$(NORMAL_INSTALL)
	test -z "$(wwwhtmldir)" || $(mkdir_p) "$(DESTDIR)$(wwwhtmldir)"
	@list='$(wwwhtml_DATA)'; for p in $$list; do \
	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
	  f=$(am__strip_dir) \
	  echo " $(wwwhtmlDATA_INSTALL) '$$d$$p' '$(DESTDIR)$(wwwhtmldir)/$$f'"; \
	  $(wwwhtmlDATA_INSTALL) "$$d$$p" "$(DESTDIR)$(wwwhtmldir)/$$f"; \
	done

uninstall-wwwhtmlDATA:
	@$(NORMAL_UNINSTALL)
	@list='$(wwwhtml_DATA)'; for p in $$list; do \
	  f=$(am__strip_dir) \
	  echo " rm -f '$(DESTDIR)$(wwwhtmldir)/$$f'"; \
	  rm -f "$(DESTDIR)$(wwwhtmldir)/$$f"; \
	done
tags: TAGS
TAGS:

ctags: CTAGS
CTAGS:


distdir: $(DISTFILES)
	@srcdirstrip=`echo "$(srcdir)" | sed 's|.|.|g'`; \
	topsrcdirstrip=`echo "$(top_srcdir)" | sed 's|.|.|g'`; \
	list='$(DISTFILES)'; for file in $$list; do \
	  case $$file in \
	    $(srcdir)/*) file=`echo "$$file" | sed "s|^$$srcdirstrip/||"`;; \
	    $(top_srcdir)/*) file=`echo "$$file" | sed "s|^$$topsrcdirstrip/|$(top_builddir)/|"`;; \
	  esac; \
	  if test -f $$file || test -d $$file; then d=.; else d=$(srcdir); fi; \
	  dir=`echo "$$file" | sed -e 's,/[^/]*$$,,'`; \
	  if test "$$dir" != "$$file" && test "$$dir" != "."; then \
	    dir="/$$dir"; \
	    $(mkdir_p) "$(distdir)$$dir"; \
	  else \
	    dir=''; \
	  fi; \
	  if test -d $$d/$$file; then \
	    if test -d $(srcdir)/$$file && test $$d != $(srcdir); then \
	      cp -pR $(srcdir)/$$file $(distdir)$$dir || exit 1; \
	    fi; \
	    cp -pR $$d/$$file $(distdir)$$dir || exit 1; \
	  else \
	    test -f $(distdir)/$$file \
	    || cp -p $$d/$$file $(distdir)/$$file \
	    || exit 1; \
	  fi; \
	done
check-am: all-am
check: check-am
all-am: Makefile $(SCRIPTS) $(DATA)
installdirs:
	for dir in "$(DESTDIR)$(cronddir)" "$(DESTDIR)$(initddir)" "$(DESTDIR)$(toolsmaindir)" "$(DESTDIR)$(confdir)" "$(DESTDIR)$(confddir)" "$(DESTDIR)$(toolsmaindir)" "$(DESTDIR)$(wwwconfdir)" "$(DESTDIR)$(wwwgendir)" "$(DESTDIR)$(wwwhtmldir)"; do \
	  test -z "$$dir" || $(mkdir_p) "$$dir"; \
	done
install: install-am
install-exec: install-exec-am
install-data: install-data-am
uninstall: uninstall-am

install-am: all-am
	@$(MAKE) $(AM_MAKEFLAGS) install-exec-am install-data-am

installcheck: installcheck-am
install-strip:
	$(MAKE) $(AM_MAKEFLAGS) INSTALL_PROGRAM="$(INSTALL_STRIP_PROGRAM)" \
	  install_sh_PROGRAM="$(INSTALL_STRIP_PROGRAM)" INSTALL_STRIP_FLAG=-s \
	  `test -z '$(STRIP)' || \
	    echo "INSTALL_PROGRAM_ENV=STRIPPROG='$(STRIP)'"` install
mostlyclean-generic:

clean-generic:
	-test -z "$(CLEANFILES)" || rm -f $(CLEANFILES)

distclean-generic:
	-test -z "$(CONFIG_CLEAN_FILES)" || rm -f $(CONFIG_CLEAN_FILES)

maintainer-clean-generic:
	@echo "This command is intended for maintainers to use"
	@echo "it deletes files that may require special tools to rebuild."
clean: clean-am

clean-am: clean-generic clean-libtool mostlyclean-am

distclean: distclean-am
	-rm -f Makefile
distclean-am: clean-am distclean-generic distclean-libtool

dvi: dvi-am

dvi-am:

html: html-am

info: info-am

info-am:

install-data-am: install-confDATA install-confdDATA \
	install-crondSCRIPTS install-initdSCRIPTS \
	install-toolsmainDATA install-toolsmainSCRIPTS \
	install-wwwconfDATA install-wwwgenDATA install-wwwhtmlDATA

install-exec-am:

install-info: install-info-am

install-man:

installcheck-am:

maintainer-clean: maintainer-clean-am
	-rm -f Makefile
maintainer-clean-am: distclean-am maintainer-clean-generic

mostlyclean: mostlyclean-am

mostlyclean-am: mostlyclean-generic mostlyclean-libtool

pdf: pdf-am

pdf-am:

ps: ps-am

ps-am:

uninstall-am: uninstall-confDATA uninstall-confdDATA \
	uninstall-crondSCRIPTS uninstall-info-am \
	uninstall-initdSCRIPTS uninstall-toolsmainDATA \
	uninstall-toolsmainSCRIPTS uninstall-wwwconfDATA \
	uninstall-wwwgenDATA uninstall-wwwhtmlDATA

.PHONY: all all-am check check-am clean clean-generic clean-libtool \
	distclean distclean-generic distclean-libtool distdir dvi \
	dvi-am html html-am info info-am install install-am \
	install-confDATA install-confdDATA install-crondSCRIPTS \
	install-data install-data-am install-exec install-exec-am \
	install-info install-info-am install-initdSCRIPTS install-man \
	install-strip install-toolsmainDATA install-toolsmainSCRIPTS \
	install-wwwconfDATA install-wwwgenDATA install-wwwhtmlDATA \
	installcheck installcheck-am installdirs maintainer-clean \
	maintainer-clean-generic mostlyclean mostlyclean-generic \
	mostlyclean-libtool pdf pdf-am ps ps-am uninstall uninstall-am \
	uninstall-confDATA uninstall-confdDATA uninstall-crondSCRIPTS \
	uninstall-info-am uninstall-initdSCRIPTS \
	uninstall-toolsmainDATA uninstall-toolsmainSCRIPTS \
	uninstall-wwwconfDATA uninstall-wwwgenDATA \
	uninstall-wwwhtmlDATA

# Tell versions [3.59,3.63) of GNU make to not export all variables.
# Otherwise a system limit (for SysV at least) may be exceeded.
.NOEXPORT:
